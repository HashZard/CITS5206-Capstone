# Application
APP_NAME=Geo LLM API
APP_ENV=dev
APP_HOST=0.0.0.0
APP_PORT=8000

# Flask
SECRET_KEY=dev-secret
FLASK_CONFIG=development

# Postgres (read-only user)
POSTGRES_DSN=postgresql://readonly:readonly@localhost:5432/postgis

# OpenAI
OPENAI_API_KEY=sk-xxxx
OPENAI_DEFAULT_MODEL=gpt-4o-mini